{%  extends 'base.html.twig' %}

{% block body %}
    <h1>Hello, {{ name }}!</h1>

    {% if message is not empty %}
        {{ message }}
    {% endif %}

    {{ form(form, {'method': 'POST'}) }}
    {{ form_end(form) }}
{% endblock %}
